{
  "name": "posthog-slack-analytics",
  "version": "1.0.0",
  "description": "Automated PostHog analytics digest to Slack",
  "private": true,
  "scripts": {
    "dev": "npx tsx src/index.ts",
    "test-cron": "npx tsx -e \"import handler from './api/cron/analytics'; handler({headers:{}} as any, {status:()=>({json:console.log})} as any)\""
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.6.0"
  }
}
